Student#: student_17

Command: ssh student@10.50.35.85 -XStudent#: student_17

Command: ssh student@10.50.35.85 -X

Password: password

Password: password

sudo scapy (which uses the python scripting language)
use a=Ether() to set an ethernet to a variable, then use a.show() to show the ethernet or a.type=<value> to change the type value for example 
